'use strict'

class PTexte {
  static analyse(){
    console.log("Je dois analyser le texte.")
    // Il faut donc qu'il soit connu de l'application
    // Dans le cas contraire, il faut demander à le choisir
    // Ensuite, on doit jouer une ligne de commande (process_child)
  }
}

module.exports = PTexte;
